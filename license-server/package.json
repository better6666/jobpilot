{
  "name": "jobpilot-license-server",
  "private": true,
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local",
    "deploy": "wrangler deploy",
    "typecheck": "tsc --noEmit",
    "test": "node --experimental-strip-types test/cards.test.ts && node --experimental-strip-types test/relay.test.ts",
    "db:init": "wrangler d1 execute jobpilot-license --local --file schema.sql"
  },
  "dependencies": {
    "hono": "^4.6.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^5.20260918.1",
    "typescript": "^5.5.0",
    "wrangler": "^4.0.0"
  }
}
